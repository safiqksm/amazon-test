package soujava;

import java.util.List;


public class DistancePairNumbers {

	public void calculateMinimalDistanceBetweenIndexes(List<Integer> mainListToSearchDistance, List<String> pairList) {
		
		for (String pair : pairList) {
			
			int[] pairsToBeFound = getPairNumbersAsInt(pair);
			
			findMinimalDistanceInList(pairsToBeFound[0], pairsToBeFound[1]);
		}
		
	}

	private void findMinimalDistanceInList(int firstNumberOfPair, int secondNumberOfPair) {
		
		
		
	}

	private int[] getPairNumbersAsInt(String pair) {
		String[] pairs = pair.split(" ");
		
		int firstNumberOfPair = Integer.parseInt(pairs[0]);
		int secondNumberOfPair = Integer.parseInt(pairs[1]);
		
		return new int[]{firstNumberOfPair, secondNumberOfPair};
	}

}
